import { AssetIconPipe } from './asset-icon.pipe';

describe('AssetIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetIconPipe();
    expect(pipe).toBeTruthy();
  });
});
